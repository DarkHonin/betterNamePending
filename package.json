{
    "scripts": {
        "jsx": "npx babel --watch src --out-dir public/javascript --presets react-app/prod",
        "start": "node ./bin/www",
        "sass": "sass --watch sass:public/stylesheets --no-source-map"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-react-app": "^3.1.2"
        
    },
    "dependencies": {
        "express": "^4.17.1",
        "pug": "^3.0.0"
    }
}
